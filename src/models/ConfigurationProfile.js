export class ConfigurationProfile {
  constructor(id, displayName, modifiedDate, assignments) {
    this.id = id;
    this.displayName = displayName;
    this.modifiedDate = modifiedDate;
    this.assignments = assignments;
  }
}
